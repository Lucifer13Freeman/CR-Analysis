export class ReadExcelDto
{
    // readonly data: Array<T> = [];
    // readonly header?: Array<string>; 
    // readonly filename?: string;
    // readonly extension?: 'xlsx' | 'csv' = 'csv';
    readonly file?: File;
}